.comment-show-box
  = link_to user_path(comment.user) do
    %p.comment-user= "#{comment.user.name}さん"
    = simple_format( comment.content )
  - if current_user.id == comment.user.id
    = link_to post_comment_path(comment.post, comment), { method: :delete, class: "comment-delete-icon", remote: true, data: { confirm:"削除しますか？" } } do
      %i.far.fa-trash-alt
